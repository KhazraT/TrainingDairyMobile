<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TrainingDairyMobile" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TrainingDairyMobile" parent="Base.Theme.TrainingDairyMobile" />

    <!-- Splash screen theme - без Action Bar и с фиксированным фиолетовым фоном -->
    <style name="Theme.TrainingDairyMobile.Splash" parent="Theme.TrainingDairyMobile">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowBackground">@color/splash_background</item>
    </style>
</resources>